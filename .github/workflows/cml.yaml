name: Dummy project
on: [push]
jobs:
  train-and-report:
    runs-on: ubuntu-latest
    container: docker://ghcr.io/iterative/cml:0-dvc2-base1
    steps:
      - uses: actions/checkout@v3
      - name: Train model
        run: |
          python deploy.py  # generate plot.png

          # Create CML report
          cat metrics.txt >> report.md
          cml comment create report.md